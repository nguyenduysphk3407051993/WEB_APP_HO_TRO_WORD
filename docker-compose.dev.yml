# Override cho dev local: expose backend ra ngoài để gọi trực tiếp /docs
# Dùng: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
services:
  backend:
    ports:
      - "8000:8000"

  frontend:
    ports: !override
      - "3000:80"
